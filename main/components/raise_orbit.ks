@lazyGlobal off.

parameter desiredOrbit, // Altitude we wish to raise our orbit to
          burnAt.       // A timestamp for which to center the burn "node",
                        // which will be the altitude opposite of the burn
